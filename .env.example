# Configuración del Sistema
APP_BASE=/
PORT=80

# Base de Datos (Docker)
DB_HOST=db
DB_NAME=sistema_pqrs
DB_USER=pqrs_user
DB_PASSWORD=tu_password_seguro
DB_ROOT_PASSWORD=root_password_seguro

# Correo SMTP (Configura aquí en producción)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=
FROM_NAME="Sistema PQRS"
